kind: pipeline
name: default

steps:
- name: test buildah-plugin
  image: registry.example.com/buildah-plugin
  pull: if-not-exists
  settings:
    log_level: debug
    access_key: abc
    secret_key: 123
    registry: public.ecr.aws
    public_registry: true
    repo: kanopy/buildah-plugin
    commands:
    - version
    - manifest
    manifest_sources:
    - git-1
    - git-2
    manifest_targets:
    - git-1-2-combined
    - git-1-2-combined-2
